@model IEnumerable<AutotinklasInformacinėSistema.Saskaita>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Sąskaitos data")
        </th>
        <th>
            @Html.DisplayName("Sąskaitos suma")
        </th>
        <th>
            @Html.DisplayName("Užsakymo numeris")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.data)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.suma)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Uzsakymas.uzsakymo_nr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.saskaitos_numeris }) |
            @Html.ActionLink("Details", "Details", new { id=item.saskaitos_numeris }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.saskaitos_numeris })
        </td>
    </tr>
}

</table>
@Html.ActionLink("Grįžti į valdymą", "MainRoot", "Home")